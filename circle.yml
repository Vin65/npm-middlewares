machine:
  node:
    version: 8.1.2
dependencies:
  override:
    - npm install --verbose
test:
  override:
    - npm run test
  post:
    - if [[ -e test-resutls.xml ]]; then cp test-results.xml $CIRCLE_TEST_REPORTS/test-results.xml; fi